import { NoRequestPageDirective } from './no-request-page.directive';

describe('NoRequestPageDirective', () => {
  it('should create an instance', () => {
    const directive = new NoRequestPageDirective();
    expect(directive).toBeTruthy();
  });
});
